plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.demoproject"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17  // เปลี่ยนเป็น Java 17
        targetCompatibility = JavaVersion.VERSION_17  // เปลี่ยนเป็น Java 17
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_17  // เปลี่ยนเป็น Java 17
    }

    defaultConfig {
        applicationId = "com.example.demoproject"
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
